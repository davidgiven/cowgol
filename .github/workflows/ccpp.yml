name: C/C++ CI

on: [push]

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: apt
      run: sudo apt install ninja-build lua5.2 pasmo libz80ex-dev:i686 flex libbsd-dev:i686 libreadline-dev:i686 bison
    - name: make
      run: make

