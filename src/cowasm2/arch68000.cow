include "arch68000.coh";

@impl sub Emit16 is
	Emit8((w >> 8) as uint8);
	Emit8(w as uint8);
end sub;

@impl sub Emit32 is
	var d := n.number;
	Emit16((d >> 16) as uint16);
	Emit16(d as uint16);
end sub;

Main("68000 flat binary assembler (c) 2021 David Given");

