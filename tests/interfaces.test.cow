include "cowgol.coh";
include "_framework.coh";

interface Interface(in1: uint8, in2: uint32): (out1: uint32, out2: uint8);

sub Fnord implements Interface is
	var q: uint8 := 9; # in Fnord's workspace
	out1 := in2; # parameters are in Interface's workspace
	out2 := in1;
end sub;

sub Baz implements Interface is
	var p: uint8 := 9; # in Baz's workspace
end sub;

Fnord();

